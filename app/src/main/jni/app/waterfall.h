void Init_waterfall(int16_t width, int16_t height);
int Draw_waterfall(float time_per_row);
void Shutdown_waterfall();
void Draw_update(int16_t *pData, uint32_t size);
void Draw_update(float *pData, uint32_t size);